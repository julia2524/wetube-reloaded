header
    a(href="/").header__logo
        i.fab.fa-youtube
    nav   
        ul 
            a(href="/search")
                i.fas.fa-search
            if loggedIn 
                li 
                    a(href="/videos/upload") Upload Video
                li
                    a(href="/users/edit") Edit Profile 
                li 
                    a(href="/users/logout") Log Out
                li 
                    a(href=`/users/${loggedInUser._id}`) 
                        if(!loggedInUser.avatarUrl)
                            img.header__avatar(src="/defaultImg/defaultImg.jpg")
                        else
                            img.header__avatar(src=loggedInUser.avatarUrl)                 
            else
                li 
                    a(href="/login") Login
                li 
                    a(href="/join").header__btn Join

            //- if loggedIn 
            //-     div 
            //-         h3 안녕하세요, #{loggedInUser.name}님!
            //-         p 🚀 벌써 우리 사이트에 #{loggedInUser.loginCount}번이나 방문하셨네요!
            //-         p ⏰ 마지막 접속: #{loggedInUser.lastLogin}
